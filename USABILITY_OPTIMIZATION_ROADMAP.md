# SQL Agent 功能与可用性优化迭代台账

## 1. 文档目的

本文档用于管理 SQL Agent 后续功能和可用性优化，避免在迭代过程中出现范围不清、页面变化不明确、流程被悄悄改变或完成情况无法核对的问题。

本轮仅考虑实际使用体验、研发效率、运行稳定性和数据资产能力，不包含登录、认证、权限和数据授权改造。

## 2. 固定迭代规则

1. 每个迭代批次固定包含 **3 个优化点**，未经确认不临时增加第四项。
2. 开始一个批次前，先向用户确认该批次的 3 个点；用户可调整顺序或替换其中的项目。
3. 每完成一个优化点，必须立即更新本文档，补充：
   - 实际完成内容；
   - 实际影响的页面和入口；
   - 新增或改变的用户流程；
   - 接口、数据表或配置变化；
   - 自动化测试与人工验证结果；
   - 已知限制和遗留问题。
4. 一个批次的 3 个点全部完成后停止继续开发，向用户汇报实际结果，并由用户确认下一批要修复的 3 个点。
5. 未写入本文档的功能不随当前批次顺带开发。发现关联问题时只记录到“候选问题”，除非它会阻断当前功能或导致数据错误。
6. 页面调整必须说明入口、页面区域、交互前后差异；不能只描述为“优化样式”或“提升体验”。
7. 后端调整必须说明同步/异步边界、失败行为、兼容性以及是否需要 DDL。
8. 不主动启动、停止或重启用户正在运行的服务；需要真实服务验证时先说明验证范围。

## 3. 状态定义

| 状态 | 含义 |
|---|---|
| 待确认 | 已进入路线图，但尚未得到用户对该批次的确认 |
| 待开始 | 批次已确认，尚未修改代码 |
| 进行中 | 正在实现或验证 |
| 待验收 | 代码和测试已完成，等待用户确认效果 |
| 已完成 | 用户确认通过，结果已记录 |
| 阻塞 | 因环境、依赖或必要信息缺失无法继续 |

## 4. 批次总览

| 批次 | 3 个优化点 | 预期主要收益 | 状态 |
|---|---|---|---|
| 第 1 批 | 创建链路分阶段耗时；批量读取 MySQL Schema；批量持久化映射与引用 | 消除同步任务创建随表数量线性增长的主要等待 | 已完成 |
| 第 2 批 | 统一工作台；全局搜索；实时告警闭环 | 减少跨模块查找和日常巡检成本 | 已完成 |
| 第 3 批 | 统一诊断报告；实例恢复与重跑；计算/出仓真实调试 | 缩短故障定位与恢复时间 | 待验收（代码缺口已复核修正） |
| 第 4 批 | 统一血缘解析；业务域与数据资产；影响分析与实时表生命周期 | 建立可信的数据资产和上下游关系 | 待确认（4.1 仅部分实现） |
| 第 5 批 | SQL 开发体验；函数目录；调度 DAG 与 SLA | 提升开发、发布和调度效率 | 已完成 |
| 第 6 批 | 实时任务查看一致性；出仓 Schema/指标；Agent 建议闭环 | 统一实时查看、提前发现出仓漂移并安全应用 Agent 草稿 | 已完成（保留环境验收限制） |
| 第 7 批 | 统一 Java 血缘入口；不可变血缘快照与语料验真；现有业务域资产化 | 消除双解析口径，并把已有业务域数据接入资产目录 | 待验收 |
| 第 8 批 | 全局表/字段血缘；实时表 Schema 历史；结构化影响分析 | 形成可追溯的资产关系和变更证据 | 待验收 |
| 第 9 批 | 独立数据地图；Neo4j 当前生产图；可靠投影与解析监控 | 将资产、任务和字段关系升级为统一图查询工作区 | 待部署环境验收 |

### 本轮代码复核与更正（2026-09-17）

本次不以台账文字作为完成依据，逐项检查了 Controller、Service、Runner、前端入口、DDL 和测试。结论如下：

| 批次 | 实际代码结论 | 本次更正/修复 |
|---|---|---|
| 第 1 批 | 分阶段日志、批量 Schema 和批量持久化均存在，且保留真实压测记录。 | 无功能性更正；保留已完成。 |
| 第 2 批 | 工作台和搜索与描述一致；告警中心存在，但终态实例的实例级告警不会自动恢复，禁用规则也不会关闭其活动告警。 | 已补齐终态实例自动恢复、规则状态清零和禁用规则恢复活动事件。 |
| 第 3 批 | 恢复和真实调试链路存在；诊断查询错误地读取不存在的 `alert_type`，证据引用也只是文本，不能定位证据。 | 已改为真实字段 `event_type`，诊断证据引用可滚动定位，来源链接可直接打开。 |
| 第 4 批 | Java `parse-sql` 已被 backend、datacompare、realtime-backend 使用，但 Agent 仍直接使用 `sqlglot`；没有 `LineageSnapshot` 持久化；4.2、4.3 未实施。 | 撤销“4.1 已完成”的错误结论，改为“部分实现”；本次不冒充完成第 4 批。 |
| 第 5 批 | SQL 预览、函数目录、DAG/SLA/补数主体存在；但预览请求未显式传递 30 秒超时，函数列表展示了无法生效的类型筛选，无耗时样本时仍会生成任意关键路径。 | 已传递 30 秒 Hive 客户端超时、移除虚假类型筛选、无真实样本时返回空关键路径。 |
| 第 6 批 | 第一版只统一了抽屉外壳、告警和停止弹窗，实例列表与变更记录仍有两套；Runner 读取 MySQL Schema 的来源与保存阶段不一致；DDL 建议没有使用真实白名单。 | 已继续抽取统一实例列表和变更记录；Runner 改用与保存阶段一致的 `information_schema`；离线 DDL 与实时表 DDL 分别复用现有 Hive/Paimon 校验入口。 |

复核后的原则：只有代码、接口、数据结构和自动化证据都存在才写“代码完成”；需要真实集群或浏览器环境的项目继续写明“待环境验收”，不会用单元测试代替运行验收。

---

## 5. 第 1 批：同步任务创建性能

本批已参考 `realtime-data-dev` 的 `b346ec93` 修复和复测报告，但只移植适用于当前项目的同步优化。当前不引入异步校验、校验任务表或页面状态；保存时的 Schema 校验标准不降低。

### 1.1 创建链路分阶段耗时基线

- **修复内容**：统一任务入口和兼容入口的预览、创建、更新已记录结构化阶段耗时；覆盖参数/Schema 校验、配置规范化、映射冲突、Paimon 目标检查、任务与版本、映射登记、审计和事务总耗时。日志同时记录表数、MySQL JDBC 建连次数、元数据查询次数、Paimon Catalog 打开次数及目标查询次数，不记录密码和 SQL 正文。
- **带来的影响**：可从一次请求直接判断延迟位于源端 Schema、Paimon 检查还是本地事务，而不是只看到接口总耗时。
- **页面修改**：无。
- **流程变化**：无；用户仍使用原来的预览、保存和编辑流程。
- **DDL/API**：无；响应结构保持不变，仅增加后端结构化日志。
- **测试结果**：分阶段日志实际定位出两个隐藏的线性瓶颈：第一版“单连接”仍通过 `DatabaseMetaData` 对每张表执行字段和主键两次查询；第一版 `JdbcTemplate.batchUpdate` 在当前 JDBC 配置下仍逐行发送。仅合并连接、尚未消除远程查询/写入往返时，20/40/100/200 表创建 P95 分别为 `3.315s / 4.517s / 9.812s / 21.297s`，证明只统计连接次数不足以判断优化是否生效。
- **验收阻断修复**：真实预览发现数开 MySQL 不接受动态参数查询中未引用的 `maxValue` 等别名，已将参数别名统一加反引号；这只修复已有查询兼容性，不改变配置或页面。
- **最终实测**：数开测试库 20/40/100/200 表各 5 轮；每轮均执行预览、创建、详情表数核对、确认无生产实例并删除。结构化日志可解释总耗时，例如 200 表热请求中 Schema 约 `0.24–0.30s`、Paimon 目标检查约 `0.42–1.18s`、映射登记约 `0.14–0.20s`、事务约 `0.28–0.35s`。
- **状态**：代码与真实验证完成，待用户验收。

### 1.2 批量读取 MySQL 源表 Schema

- **修复内容**：`RealtimeServerService` 新增同 Server、同数据库的批量 Schema 读取；一批表共用一个 JDBC 连接，并分别通过 1 条字段查询和 1 条主键查询读取整批表，保持字段/主键顺序以及可空性、默认值、描述和扩展属性。没有采用 `COLUMNS JOIN STATISTICS`：真实数开 RDS 对该联表执行计划较差，20 表曾耗时约 `7.4s`，拆分后 200 表通常仅约 `0.24–0.30s`。`RealtimeSyncConfigValidator` 先一次取得全部 Schema，再执行原有主键、分区键、计算列和公共字段校验。单表接口继续兼容并复用批量实现。
- **带来的影响**：20/40/100/200 表保存的 JDBC 建连次数从 N 次降为 1 次，远程元数据查询从 `2N` 次降为 2 次；不会降低任何 Schema 校验标准。单表缺失仍返回具体表名。
- **页面修改**：无，错误展示方式不变。
- **流程变化**：无。
- **DDL/API**：无新增公开接口。
- **自动化测试**：新增 20/100 表一次连接、两次元数据查询、结果顺序和缺失表定位测试；Validator 新增 100 表一次批量调用测试。完整 `realtime-backend` 测试 `88` 项全部通过。
- **真实验证**：200 表请求日志确认 `connectionCount=1`、`metadataQueryCount=2`；五轮创建均通过详情表数核对。
- **状态**：代码与真实验证完成，待用户验收。

### 1.3 批量持久化映射、实时表登记和引用

- **修复内容**：保存前按最多 500 张表分块查询已有实时表；缺失实时表使用多值 SQL 一次登记并回查 ID；生产者冲突在任何映射写入前统一检查；实时表生产者使用集合更新，同步映射和 OUTPUT 引用使用多值 SQL 写入。没有只使用驱动行为不确定的 `batchUpdate`。全部操作仍处于原事务中。
- **带来的影响**：不再为每张表执行“查询登记表—更新/插入—插映射—插引用”的多次往返，避免源端 Schema 优化后本地持久化成为新瓶颈。冲突或批量写入失败仍整体回滚。
- **页面修改**：无。
- **流程变化**：无。
- **DDL/API**：无。
- **自动化测试**：新增 100 表登记、映射顺序和引用一致性测试，以及生产者冲突时无任何部分映射/引用写入测试；完整 `realtime-backend` 测试 `88` 项全部通过。
- **真实验证**：数开测试库最终 5 轮结果如下；创建接口 20/40/100/200 表 P95 均低于 3 秒。增加生产者并发条件更新后，又用最终代码补跑 200 表，创建耗时 `2.713s`。所有测试任务、映射、引用和待创建实时表均已清理，查询 `PERF-SYNC-*` 任务及生产表均为 0；未产生运行实例。

| 表数 | 预览平均 | 预览 P95 | 创建平均 | 创建 P95 |
|---:|---:|---:|---:|---:|
| 20 | 0.600s | 0.972s | 0.696s | 0.783s |
| 40 | 0.574s | 0.688s | 0.727s | 0.823s |
| 100 | 1.214s | 1.518s | 1.481s | 1.907s |
| 200 | 1.369s | 1.880s | 1.687s | 2.047s |

- **状态**：代码与真实验证完成，待用户验收。

---

## 6. 第 2 批：统一入口、搜索与告警闭环

### 2.1 统一工作台

- **当前问题**：登录后主要从 SQL 助手或各列表进入，失败实例、待处理告警、最近任务和数据新鲜度分散在不同页面。
- **提升内容**：新增统一工作台，聚合运行中/失败任务、待处理告警、延迟调度、最近访问、常用任务和数据新鲜度异常。
- **带来的影响**：用户进入平台后即可知道“今天需要处理什么”，减少逐个模块巡检。
- **页面修改**：新增 `/overview` 页面和公共导航入口；离线/实时模式均可访问。首页卡片点击后跳转到已有任务、实例、告警或资产详情。
- **流程变化**：新增“工作台发现异常—进入对应详情—处理—返回工作台确认恢复”的日常流程。
- **接口/数据变化**：新增 `/api/overview` 聚合接口，接口故障应局部降级，单个依赖失败不能导致整个首页空白。
- **验收标准**：卡片数字与来源列表一致；异常可一跳到目标对象；部分服务不可用时显示明确降级状态。
- **实际完成内容**：新增跨离线与实时的 `/overview` 聚合服务和页面，汇总 24 小时失败、活动实例、失败调度、待处理告警、异常实时表、最近任务及近 30 天常用任务。各区域独立捕获异常，单个来源失败时其余区域继续返回。
- **实际页面影响**：顶部模式切换已调整为“工作台 / 离线 / 实时”；工作台使用独立导航，登录及未知路径默认进入工作台。同步、计算、出仓和实时表列表补充 `taskId/tab/tableId` 深链，工作台可直接打开目标详情。
- **实际流程变化**：新增“进入工作台查看待处理事项—一跳进入对应离线实例、实时任务、告警或实时表”的路径；不改变任务状态和任务操作流程。
- **接口/数据变化**：新增只读 `GET /api/overview`；无 DDL。当前不批量扫描 Hive 新鲜度，避免首页因逐表远程扫描变慢，先展示已有持久化状态可确定的实时表异常。
- **自动化验证**：新增聚合完整返回与单区降级单元测试；前端类型检查、生产构建和模式路由测试通过。隔离端口 `18081` 的浏览器回归确认默认进入工作台，实时运行、待处理告警、最近任务、常用任务和异常实时表均能展示；测试库缺少离线调度表时仅离线统计区提示降级，实时区域仍可使用。
- **状态**：代码完成，等待本批集成验收。

### 2.2 全局搜索与命令面板

- **当前问题**：任务、表、实例、会话和告警只能分别进入各列表搜索，用户已知名称时仍需多次导航。
- **提升内容**：增加 `Ctrl/Cmd+K` 全局搜索，统一检索离线任务、实时任务、Hive/Paimon 表、执行实例、会话和告警。
- **带来的影响**：常用对象可以直接定位；减少在离线/实时模式和多层导航间切换。
- **页面修改**：应用顶部增加搜索入口；结果按对象类型分组，支持键盘选择和最近搜索。
- **流程变化**：新增“打开命令面板—输入关键词—直接打开详情”的快速路径，不替换现有列表筛选。
- **接口/数据变化**：新增服务端分页 `/api/search`；返回统一对象类型、标题、副标题、状态和目标路由。
- **验收标准**：常用查询 P95 不超过 2 秒；结果权限以现有可见数据为准；跳转后保留对象上下文。
- **实际完成内容**：新增统一搜索服务，覆盖离线任务、实时任务、Hive 表、实时表、离线/实时实例、当前用户会话和实时告警。名称或 ID 完全匹配优先，其次前缀和包含匹配；结果按更新时间排序并按类型分页。
- **实际页面影响**：公共顶部增加“搜索 / ⌘K”入口；命令面板支持类型切换、键盘上下选择、Enter 打开、Esc 关闭、分组结果与分类翻页。每条结果直接跳到对应编辑页、详情抽屉、实例、数据目录、会话或告警。
- **实际流程变化**：新增“任意页面按 Ctrl/Cmd+K—输入至少 2 个字符—选择结果—保留对象上下文打开”的快速路径；原列表筛选保持不变。
- **接口/数据变化**：新增只读 `GET /api/search`，统一返回 `type/id/title/subtitle/status/mode/route/updatedAt`。Hive 搜索使用独立的 4 线程执行器和 1.5 秒等待上限，Hive 超时或失败只标记该搜索组降级，不影响本地结果。无 DDL。
- **自动化验证**：新增 Hive 失败局部降级与 Hive 表路由映射单元测试；前端类型检查、生产构建及后端编译通过。隔离浏览器回归验证顶部入口、分组结果、类型总数和 Hive 1.5 秒超时降级；使用 `CDC全链路` 可同时检出 61 个实时任务和 115 个实时实例。回归发现测试库的旧 `chat_session` 缺少 `context_title`，搜索已改为兼容旧 Schema 的固定副标题并复验，不增加 DDL。
- **已知限制**：最近搜索历史尚未持久化；本批先保证统一检索与直接定位，后续可在不改接口的前提下增加本地最近搜索。
- **状态**：代码完成，等待本批集成验收。

### 2.3 实时告警闭环

- **当前问题**：告警已有列表和确认接口，但未进入实时主导航，缺少规则、服务端分页、去重、恢复、静默和与任务实例的完整关联。
- **提升内容**：完善告警规则与事件生命周期，覆盖任务失败、频繁重启、Checkpoint 失败、反压、延迟、脏数据和 Schema 变化。
- **带来的影响**：告警从“记录列表”变成可发现、可定位、可确认、可恢复的处理闭环。
- **页面修改**：实时导航新增“告警中心”；页面增加规则、事件、已恢复三个视图，并支持从告警跳转到任务和实例。
- **流程变化**：新增“规则触发—事件去重—查看证据—进入实例处理—恢复关闭”的流程。
- **接口/数据变化**：告警改为服务端分页；增加 `AlertRule`、事件指纹、首次/最近发生时间、恢复状态和静默截止时间。
- **验收标准**：相同故障不会重复刷屏；告警可关联到真实实例；故障恢复后状态自动更新；大量历史告警不会一次性加载。
- **实际完成内容**：实时告警从兼容列表升级为完整生命周期：提供当前/已恢复分页、详情证据、确认、静默、解除静默和自动恢复；相同活动故障按指纹聚合并累加发生次数。后台每分钟最多并发检查 6 个活动实例，覆盖任务失败、频繁重启、Checkpoint 失败、持续反压、源端延迟、脏数据和 Schema 变化。任务失败只判断最新生产实例，并按任务聚合，避免历史失败实例被重复拉起告警。
- **实际页面影响**：实时模式新增“运维中心 / 告警中心”；页面包含“当前告警、已恢复、告警规则”三个 Tab、服务端筛选分页、详情证据抽屉和规则配置弹窗。任务名、实例及详情按钮均可返回对应任务/实例上下文。
- **实际流程变化**：形成“规则连续采样—活动指纹去重—查看证据—确认或静默—跳转任务/实例处理—条件消失自动恢复”的闭环。原 `GET /api/alerts` 和确认接口继续保留，旧调用方不需要同步迁移。
- **接口/数据变化**：新增 `/api/alerts/page`、`/api/alerts/{id}`、确认、静默、解除静默及 `/api/alert-rules` 查询/更新接口。扩展 `rt_alert` 生命周期字段，新增 `rt_alert_rule`、`rt_alert_rule_state`；幂等升级脚本 `upgrade-20260916-alert-closure.sql` 已在当前配置的数据源连续执行两次并核验，结果为 7 条规则、状态表存在、6 个关键扩展字段及活动指纹唯一索引存在。
- **自动化验证**：告警服务 H2 测试覆盖分页、生命周期、规则校验、禁用规则恢复活动事件，以及实例转为终态后自动恢复实例级告警并清零连续采样状态；Schema Contract 已更新至 23 张实时表。后端聚合编译、定向 Maven 测试、前端生产构建及前端全量测试通过。隔离端口 `18081` 的既有浏览器回归已验证告警导航、41 条历史告警服务端分页、详情抽屉和 7 条规则展示；本次没有重启用户 backend。
- **已知限制**：本批只闭环平台内告警事件，未新增短信、电话或 IM 外部通知渠道；外部通知编排留待后续批次单独确认。
- **状态**：代码、DDL 与隔离浏览器验证完成，待用户验收。

---

## 7. 第 3 批：统一诊断、恢复与真实调试

### 3.1 统一诊断报告

- **当前问题**：离线日志、YARN/MR 诊断、实时 Flink 指标、Checkpoint、Savepoint 和 Agent 建议分散展示，用户需要自行拼接根因。
- **提升内容**：定义统一 `DiagnosticReport`，按失败阶段整理关键证据、根因、影响范围、建议动作和相关日志链接。
- **带来的影响**：失败后直接得到结构化诊断，缩短定位时间，并减少只看到原始日志却不知道下一步的问题。
- **页面修改**：离线执行详情和实时实例详情增加“诊断”Tab；报告中的表、Step、算子、Checkpoint 可跳转到原始证据。
- **流程变化**：新增“失败—自动生成诊断—查看建议—执行恢复或修改任务”的标准流程。
- **接口/数据变化**：离线和实时返回相同诊断结构；诊断生成失败不能覆盖原始执行状态和日志。
- **验收标准**：覆盖 SQL、资源、依赖、连接、Checkpoint、反压和 Schema 类故障；每条结论必须附证据来源。
- **实际完成内容**：新增离线、实时共用的不可变 `DiagnosticReport`，统一保存修订号、完整性、失败阶段、结论、影响、证据引用、建议动作和缺失证据。离线自动汇总执行快照、失败 Step、JobHistory/YARN 指标；实时自动汇总实例、启动日志、Flink 运行状态、Checkpoint 和告警。诊断规则只在证据命中时给出结论；远端事实不可用时返回 `PARTIAL/UNKNOWN`，不猜测根因。失败实例会后台补生成，历史实例首次打开按需生成，手动刷新产生新修订且不覆盖旧报告。
- **实际页面影响**：离线执行详情的“运行诊断”升级为“诊断结论 / 原始运行事实”双区；同步、计算、出仓查看抽屉增加“诊断”Tab，可选实例查看报告与证据。诊断中的恢复动作只打开确认流程，不自动修改任务或实例。
- **实际流程变化**：新增“实例失败—后台生成报告—查看证据—手动刷新或进入恢复”的路径；报告生成失败不改变执行状态，也不影响原始日志查看。
- **接口/数据变化**：新增离线和实时诊断查询/刷新 4 个接口；新增共用 `task_diagnostic_report`。升级脚本已在当前配置的数据源执行并核验表存在。
- **自动化验证**：单元测试覆盖缺表证据优先级、远端事实缺失时部分报告、Checkpoint/反压/Schema/连接规则与证据绑定；复核新增 SQL 契约断言，确保实时诊断读取真实 `event_type` 而不是不存在的 `alert_type`；后端定向测试通过。
- **复核修复**：证据引用从不可交互的编号改为可定位到报告证据卡片的链接；证据包含原始来源 URL 时提供“查看来源”。
- **已知限制**：诊断报告当前只给出可验证的规则结论，不自动执行修复；未命中规则时明确显示“无法确定”。
- **状态**：代码、DDL 与自动化验证完成，待用户验收。

### 3.2 实例恢复与失败重跑

- **当前问题**：现有启停、Checkpoint、Savepoint 和离线执行能力分散，失败后是否沿用原版本、参数和状态恢复不够明确。
- **提升内容**：离线任务支持基于不可变快照重新执行；实时任务统一 Direct、Checkpoint、Savepoint 恢复入口，并在操作前展示将使用的版本、参数和状态路径。
- **带来的影响**：恢复操作可审计且不受任务当前编辑内容影响，降低误用错误版本或状态路径的风险。
- **页面修改**：实例详情增加“重跑/恢复”入口和确认摘要；新实例明确显示来源实例与恢复方式。
- **流程变化**：新增“选择失败实例—核对快照—选择恢复方式—生成新实例—对比结果”的流程，原实例保持不可变。
- **接口/数据变化**：实例创建请求增加来源实例和恢复策略；不直接修改旧实例状态或快照。
- **验收标准**：重跑始终使用被确认的版本和参数；无可用状态路径时禁止恢复；新旧实例关系可查询。
- **实际完成内容**：离线新增快照重跑，完整复制来源实例的原始 SQL、已渲染 SQL、参数定义、参数值、业务日期、版本号与任务修订，不读取当前编辑内容。实时新增恢复选项和恢复提交，来源只能是平台管理的正式终态实例；Direct、Checkpoint、Savepoint 的可用性按来源实例事实判断，并沿用来源实例不可变配置快照。新实例保存来源实例、恢复策略及实际状态路径，旧实例保持不变。
- **实际页面影响**：离线实例列表和详情增加“按此快照重跑”及来源实例展示；实时实例列表、诊断报告增加“恢复”，确认框展示来源实例、版本、策略和状态路径，新实例显示来源关系。
- **实际流程变化**：形成“选择失败/取消/已停止实例—核对来源快照—选择可用恢复方式—创建新实例”的流程。运行中、历史导入实例以及无状态路径的 Checkpoint/Savepoint 均被阻止。
- **接口/数据变化**：新增离线重跑、实时恢复选项和恢复提交接口；`sql_task_execution` 增加 `source_execution_id/replay_strategy`，`rt_task_instance` 增加 `source_instance_id/recovery_strategy/recovery_state_path` 及索引。当前数据源已连续执行两次幂等升级，核验离线 2 列、实时恢复/调试 6 列和 2 个来源索引完整。
- **自动化验证**：新增离线不可变快照复制、来源关系、运行门禁以及实时恢复选项仅保留来源 Job 状态路径、运行实例禁止恢复测试；前端 API 契约覆盖来源实例参数。
- **已知限制**：恢复前仍会执行现有外部 Schema/连接门禁；该校验不会回写来源实例快照。真实 Flink 状态恢复需要可用的测试集群与隔离状态路径，本批未操作业务任务。
- **状态**：代码、DDL 与自动化验证完成，待用户验收。

### 3.3 计算与出仓真实调试报告

- **当前问题**：当前 dry-run 在完成校验后还会运行无业务意义、仅固定输出字符串的 Flink `print` 作业，无法代表真实计算或出仓执行计划。
- **提升内容**：调试阶段加载真实 Catalog，生成逻辑计划和物理计划，输出输入输出表、Schema 对比、目标 MySQL 连接与字段映射检查，但不写入业务数据。
- **带来的影响**：调试结果能够真实反映任务能否提交以及可能的字段、计划和连接问题。
- **页面修改**：计算和出仓任务调试结果增加“计划、表引用、Schema、连接检查、诊断”分区，并可从任务详情再次查看。
- **流程变化**：调整为“保存并校验—发起无写入调试—查看报告—通过后启用”；取消固定输出字符串的 `print` 作业。
- **接口/数据变化**：DEBUG 实例保存调试报告和 Planner 结果摘要；敏感连接信息继续脱敏。
- **验收标准**：不产生 Paimon/MySQL 业务写入；非法 SQL、缺表、Schema 不兼容、目标表漂移和连接失败均能被明确识别。
- **实际完成内容**：删除固定字符串 `realtime-sync-dry-run` 数据流作业。计算调试在 Flink/YARN 应用环境加载真实 Paimon Catalog，通过 Flink Parser/Planner 构建单路或多路 Statement Set 计划，并把保存时已经确认的受管表引用写入报告的输入/输出表，不再从 SQL 文本猜测；出仓调试建立真实 MySQL 连接，读取目标表字段和主键，同时从 Paimon Catalog 读取源 Schema，逐表校验源字段、目标字段和主键映射。调试只做元数据、连接和计划检查，不执行 INSERT、Upsert、Delete，也不读取业务行。
- **实际页面影响**：计算、出仓任务查看抽屉增加“调试报告”Tab；DEBUG 实例列表显示报告状态，报告展示输入/输出表、逐项连接与 Schema 检查、失败诊断和 Flink 执行计划。
- **实际流程变化**：保存后发起调试会在集群侧生成 `debug-report.json` 并退出；后台读取报告、保存到 DEBUG 实例，通过后仍使用现有“最新版本调试通过”启用门禁。调试异常也会尽量保存结构化失败报告。
- **接口/数据变化**：新增 DEBUG 实例调试报告查询接口；`rt_task_instance` 增加报告状态、摘要和 JSON。提交 Jar 新增 `--debug-report-file`，支持本地和 HDFS URI；密码仍只存在于受保护快照，输出继续脱敏。
- **自动化验证**：提交端测试确认报告文件生成、参数 SHA 校验和密码不输出；Schema Contract、运行服务、前端 API 契约、前端 73 项测试及生产构建通过。未连接业务表执行调试，因此没有 Paimon/MySQL 业务写入。
- **已知限制**：计算计划由 Flink `StatementSet.explain()` 同时作为逻辑/物理计划原文保存，当前页面未再拆分 Optimized RelNode 与 ExecNode 小节；真实集群隔离链路仍需用专用测试表进行最终环境验收。
- **环境验收说明**：本批已通过后端模块测试、前端组件/API 测试和生产构建。尝试在隔离端口启动新 backend 做浏览器链路回归时，执行沙箱无法解析当前配置的 MySQL 域名，服务在数据源初始化阶段退出；未为绕过该限制而启动会执行恢复逻辑的联网进程，也未重启或停止用户现有 backend。因此“查看诊断—恢复”和“调试—查看报告—启用”的真实浏览器/集群链路仍列为用户环境验收项，不把组件测试冒充成端到端结果。
- **状态**：代码、DDL 与自动化验证完成，待用户验收。

---

## 8. 第 4 批：统一血缘与数据资产

### 4.1 统一 `parse-sql` 血缘事实源

- **当前问题**：任务保存和实时 SQL 已使用 Java `parse-sql`，但 Agent 工作区仍有独立 sqlglot 血缘实现，同一 SQL 可能产生不同结果。
- **提升内容**：以 Java `parse-sql` 为唯一血缘解析事实源；任务版本保存时生成不可变血缘快照，Agent、任务页面和资产中心只消费统一结果。
- **带来的影响**：表级和字段级血缘口径一致，解析升级后可以统一回归，而不是维护两套结论。
- **页面修改**：任务血缘展示保留现有入口，但增加解析状态与诊断；不再显示来源不一致的两套结果。
- **流程变化**：版本保存后生成血缘快照；解析失败时版本仍可保存为草稿，但发布/启用按既定门禁处理。
- **接口/数据变化**：新增不可变 `LineageSnapshot`；逐步删除 Agent 中重复的血缘实现，不保留双写。
- **验收标准**：同一批 SQL 在离线任务、实时分析、资产和 Agent 中输出一致；`SELECT *` 使用元数据展开并保留诊断。
- **代码复核结论**：仅完成了 Java `parse-sql` 本身的能力扩展，以及 backend、datacompare、realtime-backend 的部分接入。`agent/app/domain/sql/lineage.py`、任务依赖和 Agent 工作区仍直接使用 `sqlglot`；backend 的任务血缘接口仍代理 Agent；仓库中不存在按任务版本保存的不可变 `LineageSnapshot`。因此当前仍有两套血缘事实源。
- **已验证部分**：`parse-sql` 当前 30 项确定性单测和 4 项参考能力测试通过；全量语料与跨解析器测试依赖外部连接，当前 3 项仍处于跳过状态，不能据此声称全量语料已验证。
- **待完成内容**：定义并持久化版本血缘快照；将 Agent 的血缘/任务依赖入口改为消费 Java 解析结果；迁移资产与页面查询；执行有凭据的全量语料和跨解析器差异回归。
- **状态**：部分实现，未完成；等待后续单独确认，不能作为第 4 批验收结果。

### 4.2 业务域、主题与资产描述

- **当前问题**：现有数据目录包含 Hive owner、字段、分区、统计、存储和新鲜度，但没有平台管理的业务域、主题、标签和术语体系。
- **提升内容**：新增业务域、主题、标签、术语及表关联；展示生产任务、消费任务、质量状态、热度和技术负责人。
- **带来的影响**：用户可按业务语义寻找数据，而不仅依赖数据库和表名；业务域不再与 Hive owner 混淆。
- **页面修改**：数据资产导航增加业务域入口；数据目录增加业务属性、上下游任务、质量和热度区域。
- **流程变化**：新增“维护业务域—关联数据表—补充术语和标签—在目录中筛选”的流程。
- **接口/数据变化**：新增业务域、主题、标签、术语和关联模型；Hive 元数据仍保持只读。
- **验收标准**：一张表可关联业务域和多个标签；业务域筛选可分页；删除业务域不会删除物理表。
- **状态**：待确认。

### 4.3 影响分析与实时表生命周期

- **当前问题**：任务页面仅展示局部血缘，实时表缺少完整 Schema 历史、生产消费拓扑和不兼容变更影响范围。
- **提升内容**：建设表/字段全局血缘和变更影响分析；实时表记录 Schema 版本、差异、快照、文件/分区指标和新鲜度。
- **带来的影响**：修改表或任务前可以知道影响哪些上下游任务、字段和出仓目标，降低变更事故。
- **页面修改**：资产和实时表详情增加“血缘、Schema 历史、影响分析、运行指标”Tab。
- **流程变化**：新增“发起变更—查看影响—处理不兼容引用—确认变更”的流程。
- **接口/数据变化**：增加表/字段级影响查询与 Schema 历史；不改变物理删表和危险字段变更的现有限制。
- **验收标准**：可从表追溯生产者和消费者；字段变化能列出受影响 SQL；不兼容变更有明确阻断原因。
- **状态**：待确认。

---

## 9. 第 5 批：研发与调度体验

### 5.1 SQL 开发体验增强

- **当前问题**：已有编辑、自动补全、校验、Explain、质量检查和版本能力，但结果预览、参数展开、Step 导航和风险提示仍分散。
- **提升内容**：增加只读限量预览、格式化、版本差异、参数展开预览、Step 大纲、扫描分区与高代价算子提示。
- **带来的影响**：用户可在发布前更直观地确认 SQL 实际执行内容和潜在成本。
- **页面修改**：离线 SQL 工作台增加 Step 大纲与预览结果区；工具栏统一格式化、版本差异、校验和 Explain 入口。
- **流程变化**：新增“编辑—参数预览—只读预览—Explain—质量检查—保存版本”的开发流程。
- **接口/数据变化**：预览只允许受控 SELECT，强制行数和超时限制；不复用正式写入执行接口。
- **验收标准**：预览不会执行 DDL/DML；多 Step 定位准确；Explain 风险可跳转到相关 SQL 区间。
- **状态**：代码完成，待用户环境验收。

#### 完成记录

- 完成日期：2026-09-16
- 实际状态：已完成
- 实际改动：新增服务端只读 SQL 预览链路；Java ANTLR 语法树仅接受 SELECT/WITH，INSERT 只提取查询部分并由服务端包裹 `LIMIT`；MCP Hive 执行器再次校验包装结构且最多读取 201 行。编辑器增加逐 Step 参数展开、数据预览和保守格式化，Step 大纲默认可见并回标具体 Step 的校验失败与 Explain 风险。风险仅在真实计划包含笛卡尔连接、全局排序或明确的分区未裁剪证据时生成。
- 影响页面与入口：离线 SQL 新建页、版本编辑页的工具栏和底部结果区。
- 新增或改变的流程：编辑 → 选择 Step → 参数预览 → 数据预览 → 校验/Explain → 质量检查 → 保存版本。
- 接口、DDL 与配置变化：新增 `POST /api/workspace/sql/preview` 与 Agent 内部 `/workspace/sql/query-preview`；无 DDL。
- 自动化测试：`parse-sql` 30 项测试通过，覆盖 SELECT、WITH INSERT 查询抽取、DELETE/非法行数拒绝；Agent 87 项、MCP 90 项、Maven 聚合测试全部通过；前端格式化幂等与 Step/注释/参数变量保留测试、全量 78 项测试及生产构建通过。
- 人工验证：未连接用户 Hive 执行业务查询，避免在未指定隔离表时读取业务数据。
- 性能或效果对比：预览默认 100 行、最大 200 行；复核后请求会显式向 Hive 客户端传递 30 秒 socket 超时，前后端均不记录 SQL 正文。该超时是客户端阻断保护，不冒充 Hive 服务端查询取消或资源回收保证。
- 已知限制：格式化采用不改变 SQL 语义的保守规则，不尝试重排复杂表达式；分区裁剪风险只在 Hive Explain 有真实证据时展示，未做文本猜测。
- 相关文件或提交：`SqlLineageParser`、`SqlQueryPreviewService`、Hive execution preview、`TaskEditorPage`、`WorkspaceBottomPanel`。

### 5.2 Hive 函数目录

- **当前问题**：后端已有 Hive Function 查询接口，但 `/functions` 仍重定向到任务页面，用户无法独立查找函数签名和示例。
- **提升内容**：新增函数目录，支持名称搜索、分类、签名、参数、返回类型、说明和使用示例，并与编辑器补全联动。
- **带来的影响**：减少离开平台查文档和因函数参数不清导致的 SQL 错误。
- **页面修改**：数据资产下增加“函数目录”；SQL 编辑器补全详情可跳转到对应函数。
- **流程变化**：新增“搜索函数—查看签名/示例—插入编辑器”的流程。
- **接口/数据变化**：复用现有函数接口，必要时增加服务端分页和详情缓存，不新增函数执行权限。
- **验收标准**：搜索、详情和补全口径一致；函数服务不可用时不影响任务编辑。
- **状态**：已完成。

#### 完成记录

- 完成日期：2026-09-16
- 实际状态：已完成
- 实际改动：`/functions` 改为正式函数目录，支持名称、默认库、服务端分页、函数详情、调用模板复制和 Hive 降级提示；backend 对搜索缓存 5 分钟、详情缓存 10 分钟并限制 512 项。详情只标准化 HiveServer2 已返回的类型、签名、参数、返回类型和示例，缺失值显示“未提供”，不猜测。
- 影响页面与入口：离线“数据资产”导航新增“函数目录”；SQL 编辑器函数侧栏增加“在函数目录中查看”。
- 新增或改变的流程：搜索函数 → 查看真实签名/原始说明 → 复制模板，或从编辑器侧栏定位详情后插入当前光标。
- 接口、DDL 与配置变化：沿用 `/api/workspace/hive/functions` 与详情接口，响应增加标准化字段和 `cacheHit`；无 DDL。
- 自动化测试：backend 缓存命中和不猜测类型测试通过；前端 TypeScript 与生产构建通过。
- 人工验证：未重启用户 backend；真实 HiveServer2 的列表和详情展示留待当前运行环境热部署后验收。
- 性能或效果对比：相同搜索 5 分钟内、相同详情 10 分钟内不重复访问 Agent/HiveServer2，缓存有界避免长期增长。
- 已知限制：Hive `SHOW FUNCTIONS` 列表不返回函数类型，当前没有可靠的服务端函数类型筛选。复核时已移除会造成“可筛选”错觉的固定“全部类型”控件；真实类型仅在 Hive 详情确实返回时展示，没有用函数名推测类型。
- 相关文件或提交：`HiveFunctionCatalogService`、`HiveFunctionCatalogPage`、`WorkspaceFunctionPanel`、`AppShell`。

### 5.3 调度 DAG、SLA 与补数进度

- **当前问题**：已有 Cron、依赖、并发策略、重试和补数接口，但主要以表单和列表呈现，缺少全局 DAG、关键路径和运行进度。
- **提升内容**：提供可视化 DAG，展示循环风险、关键路径、最近运行状态和下游影响；增加任务超时、SLA 和补数并发/进度。
- **带来的影响**：复杂任务链更易理解，补数和失败重试的影响范围更可控。
- **页面修改**：任务管理增加“调度拓扑”；任务调度抽屉增加 SLA、超时和补数策略；节点可跳转任务与实例。
- **流程变化**：新增“查看 DAG—识别阻塞/关键路径—发起补数—跟踪批次进度”的流程。
- **接口/数据变化**：扩展现有 DAG 和补数接口，不创建另一套依赖关系。
- **验收标准**：循环依赖明确阻断；DAG 状态与实例一致；补数批次可暂停查看且不重复生成实例。
- **状态**：已完成。

#### 完成记录

- 完成日期：2026-09-16
- 实际状态：已完成
- 实际改动：DAG 接口改为一次查询全部启用任务、一次查询各任务最近 20 次成功实例耗时，并在服务内计算上下游数量、中位耗时和关键路径；失败节点到下游的链路高亮，节点可直达任务、最新实例和最新补数批次。调度增加执行超时、SLA 和 `ALERT_ONLY/CANCEL` 策略；补数增加逐日明细、并发认领、暂停、恢复和仅重试失败日期。
- 影响页面与入口：任务管理新增“调度拓扑”；调度抽屉增加 SLA/超时策略和补数最大并发；补数批次可进入详情抽屉查看每日状态、暂停、恢复及失败重试。
- 新增或改变的流程：查看 DAG → 识别关键路径/失败链路 → 配置 SLA → 创建补数批次 → 按并发额度执行 → 暂停/恢复 → 仅重试失败日期。暂停不会取消已经运行的实例，默认超时策略只记录违约，不自动取消。
- 接口、DDL 与配置变化：扩展 `/api/schedules/dag`，新增补数详情、暂停、恢复和失败重试接口；新增 `sql_task_backfill_item`，为调度表增加超时/SLA 字段、为补数批次增加并发字段，并增加成功实例样本索引。幂等脚本 `upgrade-20260916-scheduling-dag-sla-backfill.sql` 已在当前配置数据源执行并二次重跑验证。
- 自动化测试：Maven 聚合测试通过；调度单元测试 13 项通过，覆盖批量 DAG/关键路径、暂停不取消实例和超时策略；前端全量 21 个测试文件、78 项测试通过，生产构建通过；调度组件测试覆盖失败链路、状态筛选和三个深链入口，API 契约覆盖 DAG 与补数控制路径。
- 人工验证：真实 MySQL 首次执行发现旧环境从未部署调度基础表，升级脚本已改为兼容空白调度环境；同时发现数据库不支持 MySQL 8 窗口函数，已改为 MySQL 5.7 兼容的单次批量查询。真实库只读冒烟成功取得 36 条耗时样本，当前活动补数批次为 0；未创建、取消或修改业务实例，未重启 backend。
- 性能或效果对比：DAG 数据从“每节点查询”收敛为节点、依赖、耗时样本三次批量查询；每个任务最多读取 20 个成功样本。补数由一次性提交全部日期改为默认最多 3 个并发运行。
- 已知限制：关键路径是基于最近成功实例中位耗时的估算；复核后当整个 DAG 没有任何真实耗时样本时返回空关键路径，不再把任意零耗时节点标成关键路径。无样本节点继续明确显示“无耗时样本”；SLA 首期仅依赖调度轮询记录违约，不新建独立告警事件；真实浏览器交互需在用户现有 backend 部署新代码后验收。
- 相关文件或提交：`OfflineSchedulingServiceImpl`、`SqlTaskMapper.xml`、`SqlTaskBackfillItemMapper.xml`、`ScheduleDagPage`、`TaskScheduleDrawer`、`upgrade-20260916-scheduling-dag-sla-backfill.sql`。

---

## 10. 第 6 批：实时任务查看一致性、出仓稳定性与 Agent 建议闭环

> 本批不修改数据验数、分区范围和忽略空主键逻辑，也不增加任何验数配置入口。

### 6.1 统一实时任务查看抽屉

- **当前问题**：同步与计算/出仓任务分别维护详情抽屉，公共的实例、监控、诊断、告警和停止操作已出现展示与行为差异。
- **提升内容**：建立统一详情抽屉外壳、告警表和停止确认组件，继续复用现有实例工具栏、日志、监控、诊断与恢复组件；类型私有 Tab 仍由各任务注入。
- **带来的影响**：三类实时任务的抽屉尺寸、告警字段、Savepoint/直接停止规则和公共交互一致，任务私有能力不丢失。
- **页面修改**：实时同步、计算、出仓任务列表的“查看”抽屉；不新增页面。
- **流程变化**：用户入口不变；计算/出仓停止实例时与同步任务一致，可明确选择 Savepoint 或允许直接停止。
- **接口/数据变化**：无 API、DDL 和数据库变化。
- **验收标准**：三类任务公共 Tab 行为一致；同步数据链路、计算 SQL/计划、出仓映射/报告仍完整可用。
- **状态**：代码完成，待用户环境验收。

#### 完成记录

- 完成日期：2026-09-17
- 实际状态：代码完成，待用户环境验收
- 实际改动：新增统一 `RealtimeTaskDetailDrawer`、`RealtimeAlertTable`、`RealtimeInstanceStopModal`、`RealtimeInstanceList` 和 `RealtimeChangeLogTable`；同步与计算/出仓共同使用统一抽屉、实例筛选/排序/日志入口、变更记录、告警列和停止规则。同步任务补回版本 Tab；类型私有的数据链路、脏数据、Schema 变更、SQL/计划和出仓映射保持独立。
- 影响页面与入口：实时同步、计算、出仓任务列表的“查看”抽屉。
- 新增或改变的流程：入口不变；计算/出仓原先固定 Savepoint 的确认框改为与同步一致的可解释停止确认。
- 接口、DDL 与配置变化：无。
- 自动化测试：复核后的前端全量 `23` 个测试文件、`82` 项测试通过，TypeScript 检查和生产构建通过；新增共享实例筛选/排序确定性测试，公共抽屉继续复用既有日志、监控和诊断组件测试。
- 人工验证：未重启用户 backend。当前 `5173` 端口运行的是未包含本分支改动的旧前端；隔离启动本分支前端后，登录代理目标返回连接拒绝，无法在不重启用户 backend 的约束下完成真实数据浏览器流程，因此未把浏览器冒烟标记为通过。
- 性能或效果对比：公共外壳、实例列表、变更记录、告警表和停止弹窗由两套实现收敛为一套，类型私有内容保持独立。
- 已知限制：同步任务的富变更详情与数据链路仍属于私有能力，不强制套用计算/出仓的简化详情。
- 相关文件或提交：`RealtimeTaskDetailDrawer`、`RealtimeAlertTable`、`RealtimeInstanceStopModal`、两类任务详情组件。

### 6.2 出仓 Schema 一致性与 MySQL Sink 指标

- **当前问题**：保存与集群调试/启动使用的出仓 Schema 校验深度不一致，且 MySQL Sink 缺少积压、提交、重试和刷新耗时指标。
- **提升内容**：建立公共 Schema 兼容规则，补充长度、精度、unsigned、时间精度等元数据；版本保存 Schema 指纹；Sink 暴露专属指标。
- **带来的影响**：保存、调试和启动结论一致，目标表漂移可在写入前识别，运行期可定位积压和重试。
- **页面修改**：只增强现有出仓调试报告和运行监控，不新增页面或 Tab。
- **流程变化**：保持“保存—无写入调试—启用—监控”。
- **接口/数据变化**：版本 JSON 与运行快照扩展；不新增表和 DDL。
- **状态**：代码完成，待用户环境验收。

#### 完成记录

- 完成日期：2026-09-17
- 实际状态：代码完成，待用户环境验收
- 实际改动：新增唯一的 `ExportSchemaCompatibility` 规则；MySQL 元数据补充完整类型、长度、精度、小数位、unsigned 和时间精度；保存版本写入源/目标 Schema 摘要及指纹；Runner 在调试和启动时复用同一规则并识别 `MYSQL_SCHEMA_DRIFT`。复核时进一步把 Runner 的当前 MySQL Schema 读取从 JDBC `DatabaseMetaData` 改为与保存阶段一致的 `information_schema.COLUMNS/STATISTICS`，并用当前 Paimon Schema 的主键生成当前指纹，避免主键漂移被历史配置掩盖。MySQL Sink 增加待提交、已提交、批次、重试、刷新耗时、最近成功和失败批次指标。
- 影响页面与入口：出仓任务现有“调试报告”和“运行监控”；不新增页面或 Tab。
- 新增或改变的流程：流程仍为“保存—无写入调试—启用—监控”，但三个阶段现在使用相同 Schema 兼容结论；旧版本无指纹时显示“无历史基线”并继续按当前 Schema 校验。
- 接口、DDL 与配置变化：版本配置 JSON 增加 `schemaContracts`，运行快照增加 `export` 指标分组；无 DDL、无数据库字段变化。
- 自动化测试：公共兼容规则覆盖字符长度、Decimal 精度/小数位、unsigned、主键提取和指纹稳定性；任务定义测试覆盖批量目标 Schema 与版本契约；运行测试覆盖 export 指标分组；复核后的 Maven 定向测试和全模块编译通过，`parse-sql` 37 项中 34 项通过、3 项外部语料测试跳过；前端 82 项测试及生产构建通过。
- 人工验证：未停止或重启用户 backend，未连接或写入业务 MySQL 表。
- 性能或效果对比：同一批出仓映射的目标 Schema 改为一次批量读取；保存、调试和启动不再分别维护三套类型判断。
- 已知限制：指标需部署新 Runner 后由真实 Flink 作业上报；历史版本没有指纹时无法判断“从哪个历史结构漂移”，只校验当前兼容性。
- 相关文件或提交：`ExportSchemaCompatibility`、`RealtimeServerService`、`RealtimeTaskDefinitionService`、`ExportTaskRunner`、`RealtimeRuntimeService`、`RealtimeRuntimeMonitor`。

### 6.3 Agent 修改建议的安全应用闭环

- **当前问题**：现有 Proposal 可展示和应用草稿，但历史恢复、字段白名单、结构化应用结果及应用前验证尚未形成统一约束。
- **提升内容**：恢复历史建议卡片，建立目标/字段注册表，增加验证建议和明确的应用状态；继续禁止 Agent 自动保存、发布和启停。
- **带来的影响**：建议刷新后可追溯，越权字段被拒绝，用户能看到真实验证边界。
- **页面修改**：仅增强现有 Agent 抽屉。
- **流程变化**：生成建议—查看差异—验证建议—应用草稿—原页面保存或发布。
- **接口/数据变化**：新增无持久化实时任务验证接口；不新增 Proposal 表和 DDL。
- **状态**：代码完成，待用户环境验收。

#### 完成记录

- 完成日期：2026-09-17
- 实际状态：代码完成，待用户环境验收
- 实际改动：Proposal SSE 使用工具调用 ID 作为稳定 `proposalId`；历史消息把 `platform_proposal_present` 标记为 Proposal 并从 JSONL 还原原卡片；新增统一 Proposal Target Registry，校验页面类型、建议类型、字段路径、只读字段和字段类型；卡片增加 `APPLIED/READ_ONLY/STALE/INVALID/UNSUPPORTED` 状态、失败字段和“验证建议”。复核时补齐同步、计算、出仓和实时表表单的嵌套字段类型契约；离线 DDL 改为调用版本发布正在使用的 Hive `ALTER TABLE ADD/CHANGE` 白名单，实时表 DDL 复用现有 Paimon 建表解析器。
- 影响页面与入口：仅现有页面 Agent 抽屉；离线 SQL/DDL、实时计算和出仓分别展示真实验证边界，不新增独立页面。
- 新增或改变的流程：生成建议后先验证；验证不通过不应用；应用只进入页面草稿，并清除已有 SQL 分析、预览或连接测试状态，仍需用户使用原页面保存、发布或启用。
- 接口、DDL 与配置变化：新增 `POST /v1/api/tasks/validate`，仅调用现有计算/出仓校验服务，不持久化任务、版本或实例；Proposal 继续存放现有 JSONL，无 DDL。
- 自动化测试：Agent Python 3.12 完整测试 `23` 项通过，SSE 用例验证稳定 ID；backend `HistoryServiceImplTest` 4 项通过，验证 Proposal 标记与输入恢复；前端注册表用例覆盖未授权/只读字段、过期、页面不支持和历史恢复；实时 controller 用例验证无持久化校验返回引用；相关 Maven、前端全量测试与生产构建均通过。
- 人工验证：未重启用户 backend；未自动保存、发布、启停或删除任何任务。
- 性能或效果对比：历史恢复不新增数据库查询；实时出仓验证复用 6.2 的批量 Schema 查询和兼容规则。
- 已知限制：表单类建议的注册表校验覆盖字段契约，最终必填与联动规则仍由原页面 Form 在保存前再次校验；离线 DDL 只确认平台版本发布实际支持的 Hive `ALTER TABLE ADD COLUMNS/CHANGE COLUMN`，实时表 DDL 只确认受支持的 Paimon `CREATE TABLE` 结构，均不声称已在 Hive/Paimon 执行。
- 相关文件或提交：`proposalRegistry`、`AiAssistantDrawer`、`RouteAiAssistant`、`HistoryServiceImpl`、Agent `EventMapper`、`RealtimeUnifiedTaskController`。

---

## 11. 第 7 批：血缘事实源、版本快照与业务域资产化

本批只处理当前源码中能够确认的三个缺口，不实施全局影响分析、Schema 生命周期或新的标签/术语体系。

### 7.1 以 Java `parse-sql` 统一血缘查询入口

- **当前实际情况**：离线任务保存的 `TaskSqlStructureService`、实时计算的 `ManagedSqlAnalyzer` 和 datacompare 已使用 Java `parse-sql`；但 `WorkspaceController` 的任务血缘和任务依赖仍通过 `AgentProxyService` 调用 Agent，Agent 的 `lineage.py` 和 `task_dependencies.py` 使用 `sqlglot`。同一 SQL 目前确实存在两套表血缘结论。
- **修改内容**：在 backend 新增统一血缘服务，直接加载离线任务或指定版本 SQL，调用现有 `SqlLineageParser + BackendHiveTableMetadataProvider`；任务依赖也依据统一解析结果计算。现有 `/api/workspace/tasks/{taskId}/lineage` 和 `/dependencies` 路径保持兼容，不再把事实解析转发给 Agent。Agent 的相关工具改为消费 backend 结果；`sqlglot` 可继续用于补全或安全检查，但不再作为任务血缘事实源。
- **带来的影响**：离线任务保存、血缘面板、任务依赖和 Agent 查询使用同一解析器；后续修复语法时只维护一套血缘规则。
- **页面修改**：不改变血缘面板布局；来源文案改为 Java `parse-sql`，继续展示完整性和诊断。输入输出图、任务依赖图入口不变。
- **流程变化**：无新增操作；用户打开血缘或依赖时直接获得统一结果。Hive 元数据不可用时保留表级结果和明确诊断，不回退到 `sqlglot` 生成另一套结果。
- **API/DDL**：保留现有公开 API 和响应主体；本点不执行 DDL。
- **验收标准**：同一任务/version 在保存结构、血缘接口、依赖接口和 Agent 中的输入输出表完全一致；backend 不再调用 Agent 的 lineage/dependencies 路由。

#### 完成记录

- 完成日期：2026-09-17
- 实际状态：代码完成，待用户环境验收
- 实际改动：backend 新增 `TaskLineageQueryService`，血缘和任务依赖均直接复用 `TaskSqlStructureService` 的 Java `parse-sql` 解析结果；删除 `AgentProxyService` 中的血缘/依赖代理。Agent 删除独立的 `sqlglot` 血缘和依赖实现，改为只读取 backend 的统一结果。
- 影响页面与入口：离线 SQL 工作台原“血缘”面板入口和布局不变；增加事实来源、解析器版本、完整性和诊断信息。
- 新增或改变的流程：普通查看默认读取版本快照；Hive 元数据不可用时保留部分结果，不回退到另一套解析器。任务依赖继续由原入口打开，但边由 Java 血缘事实计算。
- 接口、DDL 与配置变化：保留 `GET /api/workspace/tasks/{taskId}/lineage` 和 `/dependencies`；新增 `POST /api/workspace/tasks/{taskId}/lineage/reanalyze` 用于当前解析器临时对比。本点无 DDL。
- 自动化测试：新增统一查询、依赖计算及完整表/列事实测试；backend 与 realtime-backend 目标测试通过，Agent 全量 `79` 项通过，前端 `83` 项通过并完成生产构建。
- 人工验证：未重启用户 backend；未对现有任务执行保存、发布或运行操作。
- 性能或效果对比：任务血缘不再发生 backend → Agent → `sqlglot` 的第二次解析；相同版本优先读取数据库快照。
- 已知限制：跨任务依赖仍按现有单次最多 `500` 个任务扫描，超过时明确返回结果不完整；Hive 元数据读取仍复用现有 Agent 只读元数据接口。
- 相关文件或提交：`TaskLineageQueryService`、`WorkspaceController`、Agent `backend_lineage.py`、`WorkspaceLineagePanel`。

### 7.2 不可变版本血缘快照与真实语料验真

- **当前实际情况**：离线已有 `sql_task_version_step.input_tables/output_tables`，实时版本已有完整 `config` JSON，但都没有保存完整 Statement、列级血缘、字段用途、诊断和解析器版本；历史版本查看时仍可能按新解析器重新计算。现有外部语料/跨解析器测试共 3 项因连接条件未满足而跳过，不能视为已经验证全部数开 SQL。
- **修改内容**：新增统一的版本血缘快照表，保存任务范围、任务/版本标识、SQL 校验和、方言、解析器版本、完整状态、完整 lineage JSON 和 diagnostics JSON。离线和实时版本保存时在进入数据库事务前完成解析，事务内随版本一起写入；旧快照永不覆盖。历史版本没有快照时首次查看只生成一次补录快照，并标明 `BACKFILLED`。
- **正确性验证**：使用数开连接读取全部非空任务 SQL，记录任务 ID、SQL 哈希、解析状态和诊断，不输出 SQL 正文；对 `/opt/project/spring/Antlr4Study`、`/opt/project/gs/parse-sql-relation` 与当前解析器都支持的“输入表/输出表”能力做差异比较。不一致时自动判断 CTE、临时表、默认库和动态表名等可解释差异；无法判断的只记录任务 ID 和三方结果，交由用户确认，不能拿参考项目结果直接当真值。
- **`SELECT *`**：继续通过现有 Hive 元数据适配器和实时 Paimon Schema 展开；元数据缺失时保存部分成功快照及 `WILDCARD_NOT_EXPANDED/METADATA_UNAVAILABLE` 诊断，不伪造字段。
- **页面修改**：现有血缘面板增加“版本快照/当前解析”“解析器版本”“完整/部分”信息和诊断查看；不新增独立大页面。
- **流程变化**：保存版本时形成不可变血缘事实；查看历史版本默认读取当时快照，只有用户显式选择“按当前解析器重新分析”时才产生临时对比结果，不回写历史。
- **API/DDL**：新增血缘快照查询和重新分析接口；增加幂等升级 SQL。保留 `sql_task_version_step` 供现有 Step 执行链使用，但输入输出必须由同一次解析结果生成。
- **验收标准**：历史快照不随解析器升级变化；同一 SQL/元数据只解析一次并复用；全量语料无未捕获异常，每条不支持或部分成功记录明确位置诊断。

#### 完成记录

- 完成日期：2026-09-17
- 实际状态：代码与数据库升级完成，待用户环境验收
- 实际改动：新增统一 `task_lineage_snapshot`，离线版本保存/生效与实时计算、出仓版本保存时持久化表访问、列血缘、字段用途、Join、表达式、诊断、校验和和解析器版本；历史缺失快照首次查看按 `BACKFILLED` 只补一次；显式重新分析只返回临时结果，不覆盖历史事实。实时计算快照的完整状态和顶层诊断直接继承解析器结果，不再固定标记成功。
- 影响页面与入口：血缘面板增加“版本快照/当前解析”、解析器版本、完整性、诊断数量和重新分析按钮；不新增独立页面。
- 新增或改变的流程：保存版本形成不可变快照；历史查看默认取当时快照；用户手动重新分析后可切回历史快照。
- 接口、DDL 与配置变化：新增并已在当前配置数据源执行 `upgrade-20260917-lineage-domain-assets.sql`；已核验 `task_lineage_snapshot` 的表、唯一键和查询索引。本次不执行任何任务 SQL、Hive DDL 或 Paimon DDL。
- 自动化测试：新增快照不可变、并发重复写、历史补录和实时解析状态测试；Schema Contract 覆盖表、字段和索引。数开只读语料实测读取 `242` 个任务、拆分 `2948` 条语句；三方共同进入表级比较 `1587` 条，其中 `1238` 条完全一致，当前解析器无未捕获异常。
- 人工验证：差异报告仅保存任务 ID、语句序号、SQL SHA-256、三方输入/输出和诊断，不保存 SQL 正文或连接凭据；报告固化在 `docs/testing/results/2026-09-17-sql-table-lineage-comparison.txt`，可供后续逐条判断。
- 性能或效果对比：历史血缘从每次重新解析改为按 `task/version/checksum/defaultDatabase` 命中一次快照；实时任务先完成解析与外部 Schema 校验，再进入版本写入事务。
- 已知限制：`349` 条非一致记录中，`337` 条是两个参考解析器同时异常/不支持，不能用来否定或证明当前结果，已标记待人工复核；另有 `5` 条可自动确认当前结果、`7` 条明确属于参考解析器不支持。离线版本服务沿用现有事务边界，解析与 Step/快照写入处于同一事务以保持当前并发校验语义，后续如需把远端元数据读取完全移出事务，应单独拆分事务写入器后再验收。
- 相关文件或提交：`TaskLineageSnapshotService`、`TaskLineageFacts`、`ManagedSqlAnalyzer`、`RealtimeLineageSnapshotDraft`、`CrossParserTableLineageCorpusTest`。

### 7.3 将现有 `rt_paimon_business_domain` 接入资产目录

- **当前实际情况**：数据库已经存在 `rt_paimon_business_domain`，默认包含 `c2b/b2b`，当前仅由同步任务编辑页的“目标 Paimon 表所属域”下拉框读取。Hive 数据目录和实时表详情没有业务域关联、维护入口或筛选能力，因此不能再描述为“项目完全没有业务域表”，但也不能把当前下拉配置冒充完整资产业务域。
- **修改内容**：复用 `rt_paimon_business_domain` 作为唯一业务域主数据，不再创建第二套同义 domain 表；补充描述、负责人和停用信息，并新增资产—业务域关联表，支持 `HIVE` 与 `PAIMON` 两类资产。实时表使用现有 `rt_realtime_table.id` 关联；Hive 表使用规范化的 catalog/database/table 标识，不创建或修改 Hive 物理表。
- **带来的影响**：同步任务命名使用的业务域和资产目录展示来自同一份主数据；用户可按业务域查找 Hive/Paimon 表，也能知道某个业务域下有哪些受管实时表。
- **页面修改**：数据资产导航新增“业务域管理”；现有 Hive 数据目录和实时表管理增加业务域筛选，表详情增加业务域展示/维护区域。同步任务原业务域下拉位置和流程不变。
- **流程变化**：业务域创建/编辑/停用 → 关联 Hive 或实时表 → 在资产列表按域筛选。存在关联或历史任务引用时不物理删除，只允许停用；停用后历史任务仍能回显，但新任务不能选择。
- **API/DDL**：新增业务域分页、保存、停用、资产关联和按域查询接口；提供幂等升级 SQL，保留现有 `domain_code` 和同步任务快照兼容性。
- **验收标准**：同步任务与资产页面读取同一业务域主数据；一张资产只能归属一个业务域；停用不破坏历史任务；任何业务域操作都不执行 Hive/Paimon 物理 DDL。

#### 完成记录

- 完成日期：2026-09-17
- 实际状态：代码与数据库升级完成，待用户页面验收
- 实际改动：复用并扩展现有 `rt_paimon_business_domain`，新增说明、负责人和停用时间；新增 Hive/Paimon 资产归属关系。PAIMON 关联必须引用 `rt_realtime_table.id` 并由后端反查真实库表，不能登记任意文本；资产唯一键使用完整规范化键和 SHA-256 唯一索引，兼容旧 MySQL 的索引长度限制。
- 影响页面与入口：离线“数据资产”和实时“数据源管理”导航均新增“业务域管理”；Hive 数据目录、实时表管理增加业务域筛选，表详情增加业务域维护。同步任务原“目标 Paimon 表所属域”入口不变。
- 新增或改变的流程：维护业务域 → 在 Hive 表详情或实时表详情绑定/解绑 → 按业务域筛选资产；停用只禁止新增关联，不删除历史关系。
- 接口、DDL 与配置变化：新增 `/api/realtime/business-domains` 分页、选项、保存、状态、域内资产和 assignment 接口；已执行并核验 `rt_asset_business_domain_relation`、`idx_asset_domain_id`、`idx_asset_realtime_table` 和资产唯一索引。没有创建第二套业务域主表。
- 自动化测试：Service 测试覆盖编码、停用关联拒绝、Paimon 受管表约束；Schema Contract 覆盖新增表/字段/索引；前端 API 契约覆盖分页、维护、停用、域内资产和绑定/解绑，前端全量测试与生产构建通过。
- 人工验证：只核验当前配置 MySQL 中的表、列和索引；遵守约束未重启 backend，因此本分支新页面尚未在用户现有进程做浏览器真实数据冒烟。
- 性能或效果对比：业务域和域内资产列表均为服务端分页；实时表筛选通过关系表索引查询，不加载全量资产到浏览器。
- 已知限制：首期一张资产只归属一个业务域；不包含标签、术语、跨域多归属和全局影响分析。
- 相关文件或提交：`BusinessDomainController`、`BusinessDomainService`、`BusinessDomainRepository`、`BusinessDomainsPage`、`DataCatalogPage`、`RealtimeTablesPage`。

### 第 7 批测试与约束

- Java 单测覆盖 CTE、`SELECT *`、部分元数据、依赖计算、快照不可变和历史补录；同一任务各入口增加一致性断言。
- 真实语料回归只读访问数开 SQL 和 Hive 元数据，不输出 SQL 正文，不执行任务 SQL。
- 业务域测试覆盖分页、关联唯一性、停用、历史回显和 Hive/Paimon 双资产筛选。
- 前端增加血缘快照切换、诊断展示、业务域管理和筛选的组件测试与浏览器流程测试。
- 使用独立分支 `codex/usability-batch-7`；不停止、重启或 kill 用户现有 backend；不修改验数逻辑。
- 三点完成后更新本台账并停止，等待用户验收。全局影响分析与实时表 Schema 历史留到后续批次单独确认。

---

## 12. 第 8 批：全局血缘、Schema 历史与变更影响

### 8.1 全局表级与字段级血缘

#### 完成记录

- 完成日期：2026-09-17
- 实际状态：代码与数据库升级完成，待用户页面验收
- 实际改动：新增 `task_lineage_relation`，把第 7 批不可变快照投影为表输入、表输出、字段派生和字段用途关系；离线、同步、计算、出仓的新版本均在版本事务中写入索引。历史回填每批最多 200 个，只读取现有快照 JSON；完成标记最后写入，避免中途失败留下伪完成状态。全局查询默认使用离线生效版本和实时最新版本，并补充实时受管表的直接引用。
- 影响页面与入口：数据资产新增“全局血缘”；Hive 数据目录与实时表详情新增“查看全局血缘”，支持 Catalog/库表/字段搜索、上下游方向和 1～3 层深度。
- 新增或改变的流程：任务保存版本时形成快照并同步索引；历史快照由后台幂等补索引；查询不再解析 SQL，也不扫描最多 500 个任务。索引待完成、快照不完整或节点截断时返回明确的 `complete=false` 与原因。
- 接口、DDL 与配置变化：新增 `GET /api/assets/lineage/search`、`GET /api/assets/lineage/graph`；执行 `upgrade-20260917-global-lineage-schema-history.sql` 创建关系表和来源/目标复合索引。
- 自动化测试：新增关系投影幂等测试和多字段下游合并测试；前端增加全局血缘 API 契约测试，生产构建通过。
- 人工验证：升级时当前库 `task_lineage_snapshot` 数量为 `0`，因此没有可回填的历史关系，也没有伪造历史血缘；新版本保存后会进入统一索引。未重启 backend，未保存或修改业务任务。
- 性能或效果对比：查询从“加载任务并重新组装/解析”改为读取规范化复合索引；单次关系读取设置 20,000 条和 500 节点上限，超限明确返回截断状态。
- 已知限制：现有库升级时没有第 7 批历史快照，因此旧任务要等后续保存新版本或通过原快照补录流程形成事实后才能进入全局索引；不会根据当前任务文本反向伪造历史版本。
- 相关文件或提交：`LineageRelationRepository`、`AssetLineageService`、`AssetLineageController`、`AssetLineagePage`。

### 8.2 实时表 Schema 版本与差异

#### 完成记录

- 完成日期：2026-09-17
- 实际状态：代码与数据库升级完成，待用户页面验收
- 实际改动：新增不可变 `rt_realtime_table_schema_version`；创建、手工刷新、定时刷新、安全变更和同步演进自动记录规范化 Schema、SHA-256 指纹、来源、操作人、结构化差异和兼容性。相同 Schema 只更新最后确认时间；历史基线指纹与 Java 指纹不同时先按规范化结构比对，避免首次刷新产生重复版本。
- 影响页面与入口：实时表详情新增“Schema 历史”Tab，支持查看单版本差异、字段快照及任意两个版本比较；每个版本可直接进入影响分析。
- 新增或改变的流程：创建/刷新/安全变更/同步演进后自动留存版本；不增加回滚、删列、改名、类型修改、主键/分区修改等危险入口。
- 接口、DDL 与配置变化：新增 Schema 版本分页、详情和比较接口；当前库已创建版本表、唯一键和时间索引，并为 `91` 张现有实时表各生成一个 `BACKFILLED_BASELINE`。升级脚本二次执行后仍为 `91` 个基线，幂等验证通过。
- 自动化测试：覆盖相同指纹不重复、收紧可空性判为不兼容、结构化差异和 Schema Contract；前端 API 契约及生产构建通过。
- 人工验证：只执行元数据表 DDL 和当前结构基线写入，不访问或修改 Paimon 物理表，不重启 backend。
- 性能或效果对比：重复刷新不再累积相同版本；列表只读取差异摘要，完整 Schema 仅在查看详情时加载。
- 已知限制：升级前不存在的历史结构无法恢复，本次只记录现状基线；Schema 历史从本批部署后开始连续积累。
- 相关文件或提交：`RealtimeTableSchemaVersionRepository`、`RealtimeTableService`、`RealtimeTableController`、`RealtimeTableLifecyclePanels`。

### 8.3 结构化变更影响分析

#### 完成记录

- 完成日期：2026-09-17
- 实际状态：代码完成，待用户页面验收
- 实际改动：基于规范化血缘和实时表引用统一计算离线、同步、计算、出仓任务、下游资产和字段用途；多字段变更逐字段追踪后合并，不退化成单纯表级数量。只有表级关系时明确显示“字段范围未知”；索引未完成或快照不完整时不显示成零影响。
- 影响页面与入口：实时表详情新增“影响分析”；Schema 版本行增加“查看影响”；安全变更提交前先展示影响摘要；同步任务 Schema 演进增加“查看影响”；全局血缘可从字段进入下游图。
- 新增或改变的流程：安全变更规则预检 → 影响分析 → 用户确认 → 原安全更新再次校验；兼容变更只提示，不因有消费者而阻断，现有危险变更仍禁止。影响分析不会自动停止、发布、重启或修改任何任务。
- 接口、DDL 与配置变化：新增 `POST /api/assets/impact-analysis` 和 `POST /api/realtime/tables/{id}/safe-update/validate`；本点不新增数据表。
- 自动化测试：覆盖多字段下游、字段已知/未知、实时表生命周期 API、安全变更预检契约，以及同步不兼容字段事件的影响字段提取；前端全量 `24` 个测试文件、`86` 项测试通过。
- 人工验证：未对业务任务执行安全变更或同步演进，未重启 backend；页面真实数据流程需部署本分支后验收。
- 性能或效果对比：一次影响请求只加载一次当前生产关系，多字段在内存复用同一关系集合，避免逐字段重复访问数据库。
- 已知限制：字段级影响精度取决于第 7 批快照完整性；出仓目标可定位到 MySQL 表，字段映射缺少字段级事实时会明确降级为表级影响。
- 相关文件或提交：`AssetLineageService`、`RealtimeTableImpactController`、`RealtimeTablesPage`、`SyncTaskDetailDrawer`。

---

## 13. 第 9 批：独立数据地图、图数据库血缘与解析监控

### 9.1 独立数据地图模块与统一资产入口

#### 完成记录

- 完成日期：2026-09-17
- 实际状态：代码、DDL 与自动化验证完成，待部署后页面验收
- 实际改动：新增独立 Maven 模块 `data-map`，由现有 Spring Boot 进程装配，不复制离线或实时任务服务；顶层模式从“工作台 / 离线 / 实时”扩展为“工作台 / 离线 / 实时 / 数据地图”。数据地图统一承载数据概览、数据目录、血缘分析、业务域和解析监控，原数据目录、全局血缘、业务域旧地址保留跳转，避免重复入口。
- 影响页面与入口：顶部新增“数据地图”；左侧提供“数据概览、数据目录、血缘分析、业务域、解析监控”。业务域不再同时出现在实时数据源管理中；Hive 数据目录和实时表详情仍可带当前资产上下文跳转血缘。
- 新增或改变的流程：用户从统一资产目录搜索 Hive、Paimon、MySQL 表，再进入表级、字段级或任务级血缘；任务创建、编辑、发布和运行流程不变。
- 接口、DDL 与配置变化：根工程新增 `data-map` module；新增 `/api/data-map/overview`、`/catalog/search`、`/lineage/graph`、`/impact-analysis` 和解析监控接口。旧 `/api/assets/lineage` 与实时表影响接口委托新查询服务，现有调用保持兼容。
- 自动化测试：新增数据地图路由记忆与 API 契约测试；前端共 `25` 个测试文件、`87` 项测试通过，TypeScript 与 Vite 生产构建通过。
- 人工验证：没有重启或终止现有 backend，因此本分支新页面未伪装成已完成线上浏览器验收。
- 性能或效果对比：资产和血缘入口由离线、实时两侧分散展示改为单一数据地图模式；页面查询不再扫描任务 SQL。
- 已知限制：数据地图仍复用现有登录会话和主应用壳，不是独立部署的第二套系统；这是有意设计，避免重复认证、任务模型和页面框架。
- 相关文件或提交：`data-map/pom.xml`、`DataMapController`、`DataMapOverviewPage`、`DataMapParsingPage`、`AppShell`、`mode.ts`。

### 9.2 Neo4j 当前生产血缘图与多视图查询

#### 完成记录

- 完成日期：2026-09-17
- 实际状态：实现和配置完成；当前机器 Docker daemon 未运行，真实 Neo4j 容器链路待部署环境验收
- 实际改动：MySQL `task_lineage_snapshot` 继续作为不可变事实和审计来源；新增 Neo4j 5.26 LTS 图投影，将资产、字段、任务、任务版本以及 `READS`、`WRITES`、`DERIVES_TO`、`USED_BY`、`JOINED_WITH` 关系写入当前生产图。离线只投影生效版本，实时只投影最新保存版本。查询支持表、字段、任务三种视图、上下游或双向、1～3 层深度和结构化影响分析。
- 影响页面与入口：“血缘分析”支持表级、字段级、任务级切换，点击节点查看真实属性；目录和实时表入口可定位到当前表；血缘不完整时显示缺失原因而不是“无下游”。
- 新增或改变的流程：任务版本保存 → 同事务写不可变快照与 Outbox → Neo4j 参数化 Cypher 幂等投影 → 查询当前生效代次。Neo4j 不可用时任务保存不丢事实，页面明确降级，恢复后可补投影。
- 接口、DDL 与配置变化：通过 Neo4j 官方 HTTP Query API 接入，避免主工程升级 Java 运行时；`docker-compose.yml` 增加仅本机暴露的 `neo4j:5.26-community`、持久卷和健康检查；新增 `DATA_MAP_NEO4J_*` 环境配置，默认关闭图连接，未部署 Neo4j 时不影响主业务启动。
- 自动化测试：覆盖官方 Query API 响应映射、快照到图投影、空字段不造假、生成 Cypher 管道、旧代次清理方向及 Schema Contract。
- 人工验证：`docker compose config --quiet` 通过；因本机 Docker daemon 未运行，没有启动容器，也没有把模拟 Neo4j 结果写成真实联调通过。
- 性能或效果对比：关系查询由 MySQL 多表/JSON 临时遍历改为 Neo4j 图遍历；查询限制 500 节点、1000 关系和最大 3 层，超限返回截断标记。
- 已知限制：部署时需启动 Neo4j 并设置 `DATA_MAP_NEO4J_ENABLED=true`；首次完成全量投影后才会显示真实图数据。
- 相关文件或提交：`Neo4jHttpGraphStoreClient`、`DataMapGraphProjector`、`DataMapService`、`AssetLineagePage`、`docker-compose.yml`。

### 9.3 可靠投影、全量代次与解析完整性监控

#### 完成记录

- 完成日期：2026-09-17
- 实际状态：代码、当前库 DDL 和测试完成，待部署后运行数据验收
- 实际改动：新增可靠 Outbox、任务当前状态、投影运行记录和全量代次。增量任务每分钟补齐当前生产快照；每日 02:30 或手工操作构建新代次，只有全部成功才原子切换，旧代次随后清理。失败采用有限重试，达到上限后停止自旋并允许手工重试。同步任务按源端/目标端字段顺序补齐确定性字段血缘；出仓任务按已保存映射补齐 Paimon 到 MySQL 的字段关系；外部元数据不可用时降级为部分成功，不伪造字段。
- 影响页面与入口：“数据概览”展示资产、字段、生产任务版本和待投影数量；“解析监控”展示完整、部分成功、失败、待投影、批次耗时和错误，提供补投影与全量重建。这里展示的是版本保存时的解析事实及其投影状态，不把定时投影误称为重新解析 SQL。
- 新增或改变的流程：版本事务成功后自动入 Outbox；后台认领、幂等投影、有限重试；手工全量重建只建立 `BUILDING` 代次，后台完整投影后自动激活，不让半成品覆盖当前图。
- 接口、DDL 与配置变化：已在当前配置的 MySQL 执行幂等脚本 `upgrade-20260917-data-map-neo4j.sql`，创建 `data_map_lineage_run`、`data_map_lineage_task_state`、`data_map_graph_outbox`、`data_map_projection_generation`；已核验 4 张表、字段、主键、唯一键及认领/代次/任务索引。
- 自动化测试：Java reactor 共 `275` 项通过、`3` 项外部语料条件测试跳过；其中 `data-map` `6` 项、`parse-sql` `37` 项、`datacompare` `24` 项、实时后端 `115` 项、离线后端 `93` 项。前端 `87` 项及生产构建通过。
- 人工验证：只执行平台元数据 DDL，不写业务表、不改业务任务；没有停止、重启或 kill 现有 backend。
- 性能或效果对比：新版本实时增量投影，不需要每日重扫 SQL；全量任务直接复用不可变快照。单批默认 50，可配置，查询和投影均有超时与有限重试。
- 已知限制：由于 Neo4j 容器未运行且 backend 未重启，本次无法形成真实图节点、执行浏览器端真实数据回归；部署后应先观察首次全量代次、失败重试和页面计数，再确认生产验收。
- 相关文件或提交：`LineageOutboxService`、`DataMapProjectionScheduler`、`RealtimeLineageFactEnricher`、`data_map_schema.sql`、`upgrade-20260917-data-map-neo4j.sql`。

---

## 14. 全程工程约束

- 拆分超大的实时 Service、Repository、编辑页和全局 CSS，但拆分本身不改变用户流程；相关重构随对应功能点完成，不单独制造大爆炸式改造。
- 同步、计算、出仓共享任务编辑、版本、实例、日志和诊断组件，避免三套页面逐渐产生不同交互。
- 统一离线与实时的用户状态文案；后端历史状态值保持兼容，前端使用统一展示映射。
- 所有新列表使用服务端分页；所有远端调用必须有超时、阶段耗时和明确降级结果。
- Topic 管理暂不建设。当前 `/realtime/topics` 未实现入口保持隐藏；引入真实 Kafka Source/Sink 任务链路后再整体规划。
- 每个页面改动必须增加对应自动化测试；布局类改动同时增加视觉回归断言。

## 15. 单点完成记录模板

完成任何一个优化点后，在对应条目下追加以下记录：

```markdown
#### 完成记录

- 完成日期：YYYY-MM-DD
- 实际状态：待验收 / 已完成
- 实际改动：
- 影响页面与入口：
- 新增或改变的流程：
- 接口、DDL 与配置变化：
- 自动化测试：
- 人工验证：
- 性能或效果对比：
- 已知限制：
- 相关文件或提交：
```

## 16. 当前执行门禁

- 第 5 批已于 2026-09-17 经用户确认进入下一批，台账状态更新为“已完成”。
- 第 4 批没有完成：仅 `parse-sql` 与三个 Java 调用模块的部分接入存在，Agent 统一、版本血缘快照、业务域和影响分析仍待单独确认。
- 用户已选择进入下一批，第 6 批台账状态已更新为“已完成”；其真实浏览器/集群限制仍保留在完成记录中，不改写为已做过环境验证。
- 第 7 批三个点已完成代码、DDL 与自动化验证，状态为“待验收”；停止继续开发，等待用户确认页面和环境效果。
- 第 8 批三个点已完成代码与 DDL；当前库已建立 `91` 个实时表现状基线，升级时没有历史血缘快照可回填，因此未伪造旧任务关系。等待部署后进行页面真实数据验收。
- 第 9 批三个点已完成代码、DDL、Java/前端自动化验证；当前机器 Docker daemon 未运行且遵守“不重启现有 backend”约束，Neo4j 真实图投影和新页面浏览器验收保留到部署后执行，不改写为已验证。
- 浏览器真实数据冒烟尚未标记为通过：隔离启动的新前端访问登录代理时连接被拒绝，现有 `5173` 页面又不是本分支版本；遵守“不重启/不终止用户 backend”约束，保留到用户部署本批代码后验收。
- 本批没有修改数据验数、分区范围或忽略空主键逻辑，也没有新增验数入口。
