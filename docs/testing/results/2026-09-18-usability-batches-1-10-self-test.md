# 2026-09-18 第 1～10 批联合自测报告

## 结论

本次使用当前工作区代码、项目 `local` 配置、独立 backend 端口 `8392`、独立 Agent 进程以及本机
Neo4j 4.4.46 进行联合自测。没有停止或 kill 用户已有服务，没有启动生产 Flink 作业，也没有写入业务表。

- Java、Python、前端单元测试、生产构建、Flyway 校验和 16 个浏览器视觉用例通过。
- 工作台、全局搜索、告警、诊断、SQL 函数、调度 DAG、三类实时任务列表、业务域、全局血缘、
  Schema 历史、影响分析、数据地图和平台就绪接口均返回真实数据。
- Neo4j 图存储处于 `configured/available/schemaReady/ready=true`，当前查询到 `2703` 个资产、
  `543` 个字段、`134` 个任务；解析完整 `104`、部分成功 `30`、失败 `0`、待投影 `0`。
- 发现并修复 1 个真实功能缺陷：无显式 Step 标记的单 SQL 被解析为 `Step 0`，但编辑器默认请求
  `Step 1` 且后端禁止 `0`，导致只读预览 HTTP 500。修复后 Step 0 与旧 Step 1 请求均成功返回 1 行。
- 发现 1 个尚未解决的性能问题：100 表同步任务创建 P95 复测为 `3.788s`，确认复测为
  `3.596s`，超过目标 `3s`。主要波动来自 Paimon 目标表逐表 `getTable` 的 100 次远程查询。

## 自动化基线

| 检查 | 结果 |
|---|---|
| Java reactor | `289` 项，失败 `0`、错误 `0`、跳过 `3` |
| parse-sql | `37` 项，跳过 `3` 个需要外部语料连接的条件测试 |
| datacompare | `24` 项通过 |
| realtime-backend | `118` 项通过 |
| data-map | `11` 项通过 |
| backend | `99` 项通过（含本次新增 2 项预览回归） |
| Agent pytest | `79` 项通过 |
| MCP server pytest | `90` 项通过，1 个 Pydantic forward-reference 警告 |
| Front Vitest | `25` 个文件、`88` 项通过 |
| Front build | 通过；仍有 `809.21kB`、`438.98kB` 大 chunk 警告 |
| Playwright 视觉回归 | 两档视口共 `16` 项通过 |
| Flyway validate | 通过，当前要求版本 `20260918.02` |
| shell 语法 | `db_migrate/deploy/redeploy/common/status/stop/watch_and_restart` 通过 |

## 分批结果

| 批次 | 状态 | 本次证据 | 限制或问题 |
|---|---|---|---|
| 1. 同步创建性能 | **部分失败** | 20/40/100/200 表各 5 轮；详情表数一致，测试任务均删除，无运行实例；MySQL Schema 保持 1 次连接、2 次查询 | 100 表创建 P95 `3.788s`；补跑 5 次仍为 `3.596s`。Paimon 目标检查对 100 表执行 100 次远程 `getTable`，单阶段出现 `2.409s/2.920s` |
| 2. 工作台/搜索/告警 | **通过** | `/api/overview`、`/api/search`、告警分页和 7 条规则均返回成功；工作台深链和列表布局包含在视觉回归中 | 未测试外部短信/IM 通知（本批也未实现） |
| 3. 诊断/恢复/真实调试 | **部分通过** | 离线与实时诊断报告真实返回；历史导入实例恢复明确返回 409，符合门禁；离线控制面全链路创建执行 42～45 并覆盖取消 | 当前计算/出仓样本无 DEBUG 实例，未在隔离 Flink/YARN 集群执行真实 Planner 调试和状态恢复 |
| 4. 统一血缘与资产 | **通过现有实现，外部语料未重跑** | Java 血缘单测通过；任务 23 返回 `java-parse-sql+hive-metastore`；后续第 7～9 批已补齐快照、业务域、影响分析 | 本次外部数开语料连接未获执行环境授权，3 个条件测试跳过；沿用 2026-09-17 已保存的 242 任务/2948 语句三方报告，不能写成本轮重跑 |
| 5. SQL/函数/调度 | **通过（含修复）** | `SELECT 1` 只读预览真实 Hive 返回 1 行；函数 `count` 搜索返回 3 条；调度 DAG 返回成功 | 自测发现并修复 Step 0/1 冲突；前端大 chunk 警告仍存在 |
| 6. 实时查看/出仓/Agent 建议 | **部分通过** | 同步 89、计算 1、出仓 1 个任务可分页；现有计算和出仓配置调用 `/v1/api/tasks/validate` 均通过；组件与 API 测试通过 | 未启动真实 MySQL Sink，提交/积压/重试指标和真实 Schema drift 仍需隔离 Flink 作业验收 |
| 7. Java 血缘/快照/业务域 | **通过** | 任务血缘、版本事实、2 个业务域均真实返回；数据地图解析状态无失败、无待投影 | `30` 个部分成功任务保留真实诊断，不能显示成完整血缘 |
| 8. 全局血缘/Schema 历史/影响 | **通过** | `dim_product` 搜索到 2 个资产；表级图与影响分析 `complete=true`；实时表 Schema 历史有基线版本 | 字段精度仍取决于上游快照完整性 |
| 9. 独立数据地图/Neo4j | **通过** | Neo4j 真实连接、约束、代次、目录和图查询全部成功；图谱就绪为 true | 生产仍需持续运行 Neo4j；不能退回默认关闭配置后期待图查询可用 |
| 10. 投产恢复/列表视觉/Flyway | **通过** | readiness、平台健康、Flyway validate、启动门禁、16 个视觉用例和浏览器静态资源通过 | 视觉基线为 macOS arm64 Chromium；其他平台需单独维护基线 |

## 真实流程结果

### 离线控制面

`scripts/full_e2e_test.py --control-plane-only` 成功创建测试任务 `28`，产生执行 `42/43/44/45`，
覆盖执行、禁用/启用、版本保存/生效、取消和归档。测试任务已归档，未创建新的 Hive 物理表。

### 实时同步性能

| 表数 | 预览 P95 | 创建 P95 | 结论 |
|---:|---:|---:|---|
| 20 | `1.112s` | `0.840s` | 通过 |
| 40 | `0.930s` | `1.265s` | 通过 |
| 100 | `1.922s` | `3.788s` | 未达到 3 秒目标 |
| 200 | `1.896s` | `2.852s` | 通过 |

100 表补跑创建耗时为 `3.596s / 1.932s / 1.729s / 1.099s / 0.972s`。全部 `PERF-SYNC-*`
测试任务由脚本清理，没有产生生产实例。

### 本次修复

- `SqlQueryPreviewDTO.stepNo` 允许 `0`，默认值与执行器一致。
- SQL 编辑器在结构解析完成后自动选择真实存在的首个 Step。
- 后端兼容旧前端对单语句发送 `Step 1`，多 Step 仍严格按编号选择。
- 新增两项后端回归测试；真实 Hive 同时验证请求 Step 0 和兼容 Step 1，均返回 `Step 0`、1 行数据。

## 后续处理建议

1. 下一步优先解决第 1 批性能回退：将目标库存在性检查从 N 次 `catalog.getTable` 改为一次表集合读取或有界并发/批量缓存，并重新执行 20/40/100/200 五轮基线。
2. 准备隔离 Flink/YARN 与专用 Paimon/MySQL 表后，再验收第 3、6 批的计算/出仓真实调试、恢复和 Sink 指标；当前不能用单元测试替代。
3. 如需把第 4/7 批标记为“本轮全量语料通过”，需显式允许只读访问参考源码中的数开 RDS 凭据后重跑 3 个条件测试；目前只引用 2026-09-17 的既有报告。

