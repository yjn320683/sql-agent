<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yjn.sqlagent.mapper.SqlTaskVersionStepMapper">
  <select id="listByVersion" resultType="com.yjn.sqlagent.model.entity.SqlTaskVersionStep">
    SELECT id, task_id, version_no, step_no, step_order, step_name, step_sql, statement_type,
           input_tables, output_tables, sql_checksum, create_time
    FROM sql_task_version_step
    WHERE task_id = #{taskId} AND version_no = #{versionNo}
    ORDER BY step_order
  </select>
  <delete id="deleteByVersion">
    DELETE FROM sql_task_version_step WHERE task_id = #{taskId} AND version_no = #{versionNo}
  </delete>
</mapper>
