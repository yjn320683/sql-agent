<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yjn.sqlagent.mapper.SqlTaskExecutionStepMapper">
  <select id="listByExecution" resultType="com.yjn.sqlagent.model.entity.SqlTaskExecutionStep">
    SELECT id, execution_id, task_id, step_no, step_order, step_name, source_sql_snapshot,
           rendered_sql_snapshot, status, query_id, application_ids, job_ids, error_message,
           log_file, started_at, finished_at, update_time
    FROM sql_task_execution_step WHERE execution_id = #{executionId} ORDER BY step_order
  </select>
</mapper>
